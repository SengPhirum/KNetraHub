// Docker module layer - Docker Swarm management (nodes, services, stacks,
// tasks, containers, networks, volumes, secrets, configs, registries) plus the
// GitLab stack-versioning integration, Docker SSE events, and the
// autoredeploy/usage-alert background pollers.
//
// Auto-registered by Nuxt from the layers/ directory. Pages, components,
// composables, utils and server routes here merge into the app with the same
// URLs and component names as before - only the source location changed.
export default defineNuxtConfig({
  $meta: { name: 'docker' }
})
